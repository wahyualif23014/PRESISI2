import 'package:flutter/material.dart';

class ComoditiesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("comodities Page"),
    );
  }
}

